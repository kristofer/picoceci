object Counter {
    let count: Int.
    init [
        count := 0
    ]
    inc [
        count := count + 1.
        ^ self
    ]
    value [
        ^ count
    ]
}

let c: Counter.
c := Counter new.
c inc; inc; inc.
Console println: c value printString.
