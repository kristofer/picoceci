object Counter {
    | count: Int |
    init [
        count := 0
    ]
    inc [
        count := count + 1.
        ^ self
    ]
    value [
        ^ count
    ]
}

| c: Counter |
c := Counter new.
c inc; inc; inc.
Console println: c value printString.
